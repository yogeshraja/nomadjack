{
    "task": "${taskname}",
    "script": ${script},
    "filepath": "${filepath}",
    "fileperms": "${fileperms}",
    "delete_after_exec": ${delete_after_exec},
    "override_file": ${override_file},
    "command": "${command}",
    "environment": ${environment}
}